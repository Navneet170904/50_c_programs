#include <stdio.h>
void main()
{
  int a, b, c, i, n;
  printf("Enter the number:-\n");
  scanf("%d", &n);
  a=0;
  b=1;
  c=0;
  printf("Fibonaci series:-\n");
  for(i=1; i<=n; i++)
  {
    printf("%d",c);
    a=b;
    b=c;
    c=a+b;
  }
}  
  
